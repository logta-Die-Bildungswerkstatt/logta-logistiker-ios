{
  "name": "logta-logistiker",
  "version": "1.1.0",
  "description": "LogTa LernApp – Logistiker",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Static HTML app - no build needed'",
    "cap:sync": "npx cap sync ios",
    "cap:open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
